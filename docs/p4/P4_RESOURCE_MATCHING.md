# P4 资源沉淀与匹配

会员供需和活动反馈只写正式统一资源表 `v06_market_resources`。`v04f_club_needs`、`v04f_club_offerings` 和旧匹配表仅作兼容读取，不恢复双写。

资源先进入草稿/待审核，运营人员批准后才参与匹配。首版受控规则使用资源类型、产业标签、地区和已审核 P3 关系计算分数，并保存理由、路径、风险与证据。试点运行按 `pilot_batch_id` 隔离，不扫描或改写正式资源。

匹配结果是候选，必须人工接受后才能生成 ClubLead 候选；系统不发送消息、不代替运营联系，也不自动创建 Opportunity。
