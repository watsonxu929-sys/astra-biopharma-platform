# P2.2 错误分析

1. 两个来源入口发生404：配置已修正为EMA `/en/news?page=1` 与BIO `/press-releases`，补跑成功。
2. XLSX单文档产生513个候选，全部保留待审核。需要按工作表、列语义和行数增加候选门限，不得直接扩大采集。
3. PDF部分页面预处理出现内存分配与OCR空结果告警；当前保留完整解析状态，不声称每页OCR完整。
4. 规则基线只识别监管事件和FDA机构，关键事实准确率为0，说明产品、日期、适应症等字段应由更精确规则或经评测AI处理。
5. 无API Key，AI和Hybrid没有实测分数。不得基于Rule结果推断AI效果。
6. 任务开始记录的正式库文件SHA与最终SHA不同；正式库主文件最后写入时间早于本轮试点，且试点全部显式使用副本。SQLite WAL检查点可能造成文件哈希变化，但缺少开始时一致性副本，不能宣称哈希保护项通过，已登记KNOWN_ISSUES。
