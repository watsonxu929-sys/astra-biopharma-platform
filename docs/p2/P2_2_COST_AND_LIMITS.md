# P2.2 成本与限额

- 内容硬上限30，当前配置声明19，实际11。
- AI硬上限20；默认预算5美元；价格通过环境变量配置，未配置价格时成本记0但不得解释为免费。
- 单来源并发1，请求间隔默认1秒，HTTP最多两次重试，指数退避上限4秒。
- 文档默认20MB；HTML正文80,000字符，原始HTML1,000,000字符。
- 条件请求优先使用ETag和Last-Modified；相同URL/内容哈希不重复生成业务内容。
- 当前真实AI调用0、token 0、估算成本0。
