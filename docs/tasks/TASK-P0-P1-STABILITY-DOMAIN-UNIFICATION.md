# TASK-P0-P1-STABILITY-DOMAIN-UNIFICATION

## 目标与基线

目标：恢复可信 P0 基线，在 P0 通过后以兼容方式统一核心领域模型，并清理已确认安全的冗余。

- 初始 Git：`.git` 为空，未形成仓库；已初始化 `main`，初始提交 `f03d519`，工作分支 `fix/p0-p1-stability-domain-unification`。
- 初始编译/启动：9 个 Python 文件含单个或重复 BOM，`compileall` 与应用导入失败；根因是 U+FEFF。
- 初始数据库：`data/app.db` 2,494,464 字节，初始 SHA256 `2DD7F3EE6D8EF6C658A29316C8667030777A285892AAEE9D2272F2BE8E7284D7`，导入失败前后未改变。
- 外部备份：`../biopharma-intelligence-pre-p0-p1-20260710-100816.zip`，73,458,330 字节，2026-07-10 10:08:16，SHA256 `3D1F6C2930ECA999F196AD4A0CF73F178D5A8DBA529B643B3AB61BA2FA8EB80B`。

## 修改范围

BOM/乱码修复、运行时迁移门控、标准文档、P0 验证入口、安全缓存清理；P1 复用现有 v06 正式模型和统一服务，旧表兼容读取，迁移仅在副本执行。

## 风险与回滚

不删除正式库、不修改 `.env`、不自动合并主体。代码按 P0/P1 提交回滚；数据库由人工选择任务前备份恢复。备份、补丁与疑似真实数据保留并登记。

## P0 当前结果

全量编译通过；应用可导入；22 个核心页面无 500；Uvicorn 首页与 `/health` 返回 200；临时数据库 P0 验证 7/7。后续验证统一在正式库副本运行。
