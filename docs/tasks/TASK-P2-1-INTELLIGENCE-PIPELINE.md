# TASK-P2-1-INTELLIGENCE-PIPELINE

状态：实现与副本回归完成；正式数据库迁移待维护窗口。

目标：在不建立重复主模型、不批量迁移历史数据的前提下，统一来源、采集任务、不可变快照、标准化正文、事实候选、人工审核和正式情报产品，并提供规则/AI 可插拔分析接口。

验收顺序：备份或复制数据库 → `003` dry-run → 在副本 apply 两次验证幂等 → P2 试点 → 编译/编码/基线/领域验证 → pytest → 对比正式数据库 SHA256。正式 apply 需由维护者在维护窗口执行。

回滚：停止写入 P2 服务；恢复迁移前自动备份；代码回退到 `checkpoint-p1-complete`。新增表和字段不应在正式业务表上手工 DROP。

2026-07-10 续作验收：副本迁移 dry-run/双 apply、1 条规则试点、P2 专项验证、P0 7/7、领域统一 5/5、导航 24/24、路由注册、乱码扫描和 pytest 9/9 通过。最终兼容读取暴露出旧 `db_connection()` 的 `journal_mode=WAL` 会改写正式库文件头；关键表计数与 `integrity_check` 未变化，P2 产品追踪已改为 SQLite `mode=ro`，新基线复测需保持哈希稳定。浏览器插件受 Windows ACL 阻断，页面与 API 已通过本地 HTTP 内容断言，浏览器控制台检查未执行。
