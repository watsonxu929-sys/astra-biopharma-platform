# P3 受控试点报告

批次：`P3-PILOT-20260713`。数据库：正式库的隔离副本 `C:\tmp\p3_entity_network_pilot.db`；正式库未执行迁移或试点写入。

| 项目 | 结果 |
|---|---:|
| 选取人物 | 12 |
| 选取机构 | 12 |
| 选取既有项目 | 5 |
| 试点 ProductAsset | 6 |
| 正式关系（仅副本） | 29 |
| 历史关系 | 3 |
| 多来源关系 | 2 |
| 主体解析候选 | 2 |
| 合并预览 | 1（未提交、未执行） |
| 路径查询 | 3，均有结果 |
| 连接候选 | 3 |

同名人物在无机构/职务上下文时为 weak/ambiguous；机构已审核简称命中为 medium。试点所有新增记录均标记 `is_pilot=1` 和 `pilot_batch_id`。

试点选择既有正式人物、机构和项目主档验证完整流程；新增产品、关系及三条历史时间段只存在于隔离副本，是验收数据，不代表已经进入正式业务事实。它们必须经过真实业务人员再次核验后才能考虑迁入正式库。

`scripts/verify_p3_entity_relationship_network.py` 结果：43 类型、5 分组、无孤儿证据、无非法时间、无敏感外部标识、无重定向环，路径检查小于 1 秒，完整性 `ok`。机器可读结果见 `P3_PILOT_RUN.json` 与 `P3_VERIFICATION_REPORT.json`。
