# v0.4C-1 验收清单

## 基础接入

- [ ] `/review/intake/health` 返回 `ok: true`。
- [ ] `/review/intake` 页面正常显示。
- [ ] JSON 接入后产生来源批次和来源记录。
- [ ] 相同 external_key 再次导入被识别为重复。
- [ ] 无 fields/relations 的纯文本记录标记为 `needs_structuring`。

## 自动审核任务

- [ ] 新字段生成 `fact_check` 审核项。
- [ ] 同值多来源合并到一个审核项，证据条数增加。
- [ ] 不同值生成 `conflict` 审核项。
- [ ] 推测候选显示为 `inference_pending`。
- [ ] 关系候选进入“待确认关联”。

## 正式同步

- [ ] “通过＋定为事实”的字段进入正式事实库。
- [ ] “通过＋仍为推测”的字段停留在 `waiting_fact`。
- [ ] 驳回候选标记为 `rejected`。
- [ ] 新正式值产生新版本，旧版本仍保留。
- [ ] 已确认关联进入正式关联库。

## 业务表映射

- [ ] 未启用映射时不修改原业务表。
- [ ] 启用映射后只更新指定表和指定列。
- [ ] 目标记录不存在且禁止新增时，日志显示 skipped。
- [ ] 所有同步均有日志可查。
